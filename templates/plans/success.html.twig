{% extends 'base.html.twig' %}

{% block body %}
  <div class="container text-center my-5">
    <h1 class="display-4 mb-4">{{ "Merci pour votre abonnement"|trans }} !</h1>

    <div class="card mx-auto mt-4" style="max-width: 500px;">
      {% if product.images is defined and product.images|length > 0 %}
        <img src="{{ product.images[0] }}" class="card-img-top" alt="{{ product.name }}">
      {% endif %}
      <div class="card-body">
        <h5 class="card-title">{{ product.name }}</h5>
        {% if product.description %}
          <p class="card-text text-muted">{{ product.description }}</p>
        {% endif %}

        <p class="h4">
          {{ (price.unit_amount / 100)|number_format(2, ',', ' ') }} {{ price.currency|upper }}
          <small class="text-muted">/ {{ price.recurring.interval|trans }}</small>
        </p>

        <a href="{{ path('app_home') }}" class="btn btn-success mt-3">{{ "Retour à l'accueil"|trans }}</a>
      </div>
    </div>
  </div>
{% endblock %}