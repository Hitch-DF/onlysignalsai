<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}OnlySignals AI{% endblock %}</title>

  <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('logo.png') }}">
  <link rel="shortcut icon" href="{{ asset('logo.png') }}">

  {% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
  {% endblock %}
</head>

<body class="bg-background text-foreground">

  {# Responsive Navbar partial (burger toggle handled inside) #}
  {% include "_partial/navbar.html.twig" %}

  <main class="py-4">
    <div class="container">
      {% block body %}{% endblock %}
    </div>
  </main>

  {% block footer %}
    <footer class="text-center py-4 text-muted small border-top border-secondary mt-5">
      © {{ "now"|date("Y") }} OnlySignals. {{ 'Tous droits réservés'|trans }}.
    </footer>
  {% endblock %}


  {% block javascripts %}
    {{ encore_entry_script_tags('app') }}
  {% endblock %}
</body>
</html>
